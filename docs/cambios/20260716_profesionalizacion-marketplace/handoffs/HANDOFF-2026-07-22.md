# HANDOFF — Sesión 22-jul-2026 · SPEC-05, 06 y 07 → ciclo 2 `frontend-mobile-first` COMPLETO

Bitácora de cierre. Foto fechada con evidencia; la fuente de verdad del estado vivo es la línea "Fase actual" de `CLAUDE.md`. Continúa a `HANDOFF-2026-07-21-spec04.md` (que cerró SPEC-04).

## Estado metodológico

- **Fase actual (al cerrar):** **ciclo 2 `frontend-mobile-first` CERRADO y en producción.** No hay trabajo JCC activo dentro del ciclo 2. El frontend nuevo (React 19 + Vite 8 + TS 6 + Tailwind v4 + shadcn + react-router 7, mobile-first, **PWA instalable**) se sirve en la **raíz `/`**. **SPEC-01…07 cerrados**, todos con review adversarial independiente + smoke logueado del usuario.
- **Siguiente command que toca:** **`/jcc-design` del ciclo 3 `marketplace-transactable`** (landing + webhook + Fulfillment APIs + suscripciones + gating). No arranca solo: lo decide el usuario al retomar.
- **Restricciones activas (no saltar):**
  - Frontend nuevo servido en `/`; **`web/dist` es obligatorio** para servir `/` (lo genera el CI; en local `npm run build:web`). El desarrollo usa Vite en `:5173` (proxy `/api`→:3000).
  - **Deuda transitoria del cutover (retirar en limpieza futura, NO tratar como permanente):** redirect **302 `/app→/`** en `server.js` y el alias **`/api/*` sin versión** (ya sin consumidores tras retirar el viejo). El contrato consumido es `/api/v1`.
  - Entra: la **SPA redirect URI raíz `https://<host>/`** está en uso y verificada por el smoke (login en `/` OK).
  - Se preserva todo lo de ciclos previos: backend/API/esquema/OpenAPI/migraciones, identidad bearer del ciclo 1, devBypass local, salvaguardas de captura R1, contrato de sesión y flujo de 4 fases.
- **Evidencia del estado (para reconciliar al arrancar):**
  - `main` limpio y **== `origin/main`** en `c9424e6`. Sin ramas pendientes (`2b-05-resto-flujo`, `2b-06-marco`, `2b-07-cutover` mergeadas por ff y borradas).
  - En `ciclo-2-frontend-mobile-first/`: `SPEC-01…07` + `REVIEW-SPEC-05/06/07`. `public/` **eliminada** del repo (git conserva el historial). `web/vite.config.ts` → `base:'/'`, PWA `scope`/`start_url` `/`.
  - Prod (`speech-to-prompt-xenix-hnc4ccfbfkdcdjem.westeurope-01.azurewebsites.net`): `/` sirve el SPA nuevo (`index-fSNESM5h.js`); `/app`→302→`/`; `/api/*` y `/api/v1/*` vivos.

## Qué se hizo esta sesión

Cerró el sub-ciclo **2b** entero (los 3 SPEC que faltaban) y el **cutover**, cada uno de punta a punta (especificar → implementar → revisar en bucle 3↔4 → desplegar → smoke logueado del usuario):

1. **SPEC-05 `resto-flujo`** (Revisión/Destilado/Resultado reales). Decisiones de mesa común: Destilado = solo selección de modo (default `limpio`) + huecos "Próximamente" (Ajustes/destino-formato = ciclo 4, coste = ciclo 5); **sin editor de system prompt** en el front nuevo; Revisión **reproduce audio por tramo** (consume `getSegmentAudio`); hidratación de `useCapture` desde `ActiveSession` para que "Añadir tramo" reanude la misma sesión. Review: 0 ALTA/0 MEDIA/2 BAJA (corregidas). **ADDENDUM:** fix de tipo de la fachada `distill` en `client.ts` (el `requestBody` opcional colapsaba el tipo a `never` → incallable).
2. **SPEC-06 `marco`** (Historial/Ajustes reales). Decisiones: **Ajustes sin proveedores/claves** (producto secretless; eso es ops/backoffice); **reprocess mínimo** de rescate en Historial (items con audio sin transcribir); cutover como paso aparte. Review: 0 ALTA/0 MEDIA/5 BAJA (B-1 vacío-por-filtro y B-5 deshabilitar tarjetas → corregidas; B-2/B-3/B-4 aceptadas).
3. **SPEC-07 `cutover /app→/`** (acto final de 2b). `vite base`+PWA a `/`, `web/dist` servido en `/` + redirect transitorio 302 `/app→/`, `public/` retirado; `msal.ts`/`App.tsx` sin tocar (derivan de `BASE_URL`). Review: 0 ALTA/**1 MEDIA** (bucle 302 en `/app?query` sin barra → **corregida**) /2 BAJA (aceptadas). CLAUDE.md "Architecture" actualizada (frontend = `web/` en `/`; `public/` retirado).

## Qué se verificó CON EVIDENCIA REAL

- **Estático (los 3 SPEC):** `web` build (tsc+vite) verde; lint 0 nuevos (solo 3 warnings preexistentes en `ui/{button,badge,toggle}`); `npm test` raíz **14/14** en todos.
- **e2e local (Browser integrado, devBypass + BD local):**
  - SPEC-05: guardas de deep-link (3/3), captura→revisión, edición+`PUT transcription_edited`, reproducción `GET /audio/1`, **"Añadir tramo" reanuda misma sesión** (1 `POST /sessions` + 2 `/segments`, sin duplicar), Destilado (default `limpio`, huecos), **camino de error del LLM** (Azure OpenAI 403 red privada), Resultado (render/editar/guardar/nuevo dictado vía stub de red + seed backend), dark, 0 errores de consola.
  - SPEC-06: Historial (lista agrupada + filtros + badges de estado), reabrir por fase (Completada→/result, En destilado→/review), **reprocess real** (nulé la transcripción de la sesión de prueba 13 → "En captura" + Reprocesar → `POST /reprocess 200` → re-transcribió → /review), Ajustes (cuenta devBypass, tema conmuta `.dark`+persiste, sin proveedores, logout oculto), 0 errores.
  - SPEC-07: server con el build sirve el SPA en `/` (no el vanilla), deep-links 200, `/app`→302→`/`, `/app?x=1`→302→`/?x=1` (fix MEDIA), `/api/v1` vivo.
- **Prod (curl, tras cada deploy):** el bundle servido == el build local en cada caso (SPEC-05 `index-bVTGOk0U.js`, SPEC-06 `index-CtDRVbkq.js`, SPEC-07 `index-fSNESM5h.js`); tras el cutover, `/` sirve el nuevo y el vanilla desaparece (0 `phase-container`); `/app`→302→`/`.
- **Smoke logueado del usuario en Azure (confirmado por el usuario):** SPEC-05 (happy-path real del LLM + copiado, incl. móvil); SPEC-06 (Historial/Ajustes con cuenta MSAL real); **SPEC-07 (login en `/`, flujo completo, y PWA autodetectada e instalada en móvil)**.

## Commits / deploy

- **SPEC-05:** `d9ad65e` feat · `5b3de28` fix (2 BAJA) · `909fab3` docs review · `2c1a1dc` docs deploy · `0e560bc` CERRADO. Deploy run `29926847681`.
- **SPEC-06:** `2b260cd` feat · `9a3e2d9` docs impl · `6e3ad7b` fix (B-1/B-5) · `6e6f825` docs review · `2fa82c1` docs deploy · `c940c99` CERRADO. Deploy run `29930446124`.
- **SPEC-07:** `033eca4` docs spec · `03575dc` feat · `e9b305f` fix (MEDIA redirect) · `7839361` docs deploy · **`c9424e6` CERRADO + ciclo 2 completo**. Deploy run `29933437669`.
- Todos en `main` y `origin`, árbol limpio. Sin cambios de infra de Azure (ninguno de los 3 toca infra; el cutover es routing en `server.js`+`vite`).

## Notas de la sesión (releer en frío)

- **Azure OpenAI es de red privada:** el happy-path real del destilado **no es verificable desde local** (da `403 public access disabled`). Se validó el *camino de error* en local y el happy-path por el smoke del usuario. (Para verificar Resultado en local se sembró `prompt_distilled` por el backend real + refresco de `ActiveSession`; el stub de `window.fetch` NO sirve porque openapi-fetch captura `globalThis.fetch` al crear el cliente.)
- **Lag de reinicio de App Service:** tras el deploy del cutover, el primer curl a `/` aún servía el código viejo ~segundos; re-sondear antes de concluir (memoria `project_azure_deploy_restart_lag`). Con un poll corto pasó a servir el nuevo.
- **Limitaciones del Browser pane:** los clics del `computer` fallaban por desajuste de DPR → se condujo por `.click()` en JS (que pasa por el `onClick` real); micro bloqueado → stub de `getUserMedia` con tono sintético; **Clipboard API bloqueada** → el "Copiar" muestra su fallback de error (el copiado real lo validó el usuario).
- **Fix latente de `client.ts` (SPEC-05):** la fachada `distill` era incallable (`requestBody` opcional → tipo `never`); se tipó con `components['schemas']['DistillRequest']`.
- **Semántica heredada `edited` vs `raw`:** si editas la transcripción y luego añades un tramo, el texto editado no fusiona el nuevo (modelo de datos existente; no se rediseñó — fuera de alcance).

## Pendientes heredados / cross-cutting (sin cerrar)

- **`robustez-coldstart-sql` — prueba de fuego §8.3 PENDIENTE** (no es defecto de código): pausar la BD real + smoke logueado; coordinar ventana con Agustín. Detalle en `docs/cambios/20260710_robustez-coldstart-sql/HANDOFF.md`.
- **Node 20 deprecado en el workflow:** `actions/checkout@v4` + `setup-node@v4` forzados a Node 24; bump menor (`.github/workflows/azure-deploy.yml`).
- **Limpieza futura del cutover:** retirar el redirect transitorio `/app→/` y el alias `/api/*` sin versión (ya sin consumidores).
- **Programa (ruta crítica):** burocracia de Partner Center de Agustín (verificación + fiscal + payout, semanas de latencia) — destrabar en paralelo al ciclo 3. Ciclo 4 = top-up investigación Gemini + optimizadores; hueco H6 = `users.email` sigue `NOT NULL` (abordar al retirar la lista blanca en el gate de suscripción del ciclo 3).
- **`azure-sql-multiusuario` (menores):** añadir la IP de Agustín al firewall de SQL/Storage.
- **`mejorar-destilado-limpio` (menor):** smoke funcional logueado destilando una sesión larga en `limpio`.

## Cómo retomar (próxima sesión)

1. **Reconciliar:** leer "Fase actual" de `CLAUDE.md` + esta bitácora + el índice global `docs/cambios/README.md`; confirmar `main` limpio/desplegado y sin ramas pendientes.
2. **Arrancar el ciclo 3 `marketplace-transactable`** con **`/jcc-design`** (entrevista socrática → DESIGN del ciclo). Insumos: `../DESIGN.md` del programa (§3 troceo, ciclo 3), `BRIEF-marketplace-agustin.md` (niveles de oferta + requisitos del Marketplace). Es donde entran Fulfillment APIs + webhook + gating por suscripción — decisiones estructurales a la mesa común.
3. En paralelo (negocio): destrabar Partner Center con Agustín (ruta crítica).

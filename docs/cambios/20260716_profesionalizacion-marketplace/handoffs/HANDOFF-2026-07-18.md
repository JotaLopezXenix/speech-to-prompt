# HANDOFF — Sesión 18-jul-2026 · Programa `profesionalizacion-marketplace`

Bitácora de cierre de sesión. Foto fechada con evidencia; la fuente de verdad del estado vivo es la línea "Fase actual" de `CLAUDE.md`.

## Estado metodológico

- **Fase actual:** programa multi-ciclo. **Ciclo 1 `identidad-entra` = CERRADO** (implementado, revisado LIMPIO, desplegado y verificado e2e en prod). **Trabajo en curso = ciclo 2 `frontend-mobile-first`, en ANÁLISIS (cerrado con DESIGN.md).**
- **Siguiente command que toca:** NO es un `/jcc-*` directo. Lo siguiente es la **actividad 2a (diseño)**: dirección visual + design system + maquetas estáticas de pantallas clave + decidir **wizard-vs-pantalla-única**. Se conduce vía **Claude Design** (lo maneja el usuario en `claude.ai/design`; handoff a Claude Code) **o** generando **artifacts HTML** desde Claude Code. **Solo cuando el diseño esté elegido → `/jcc-spec` del 2b** (construcción).
- **Restricciones activas (no saltar):**
  - **Decisiones estructurales del ciclo 2 ya ratificadas** (no reabrir): stack **React + Vite + TypeScript + Tailwind + shadcn/ui**, PWA instalable; móvil = **flujo completo** (paridad); **cosechar** (portar `audio-recorder.js`/`audio-guards.js`/`diagnostics.js` y **re-expresar** las salvaguardas de `phase1-capture.js` preservando comportamiento — R1, mayor riesgo); **formalizar API** = OpenAPI + cliente tipado + alias `/api/v1`, **sin reescribir el backend**; apps nativas = ciclo futuro.
  - **Qué se PRESERVA en el ciclo 2** (ver `ciclo-2-frontend-mobile-first/DESIGN.md §5`): backend y API `/api/*` (comportamiento intacto), flujo de 4 fases, contrato de sesión (`segments[]` + `transcription_raw/edited` materializados), **salvaguardas de captura** (no perder audio, banner Reintentar, warm-up, `diagnostic_events`, sin falsos positivos), Ajustes e Historial, e integración con la identidad bearer del ciclo 1.
  - **R2 a resolver en el SPEC de 2b:** integración de ramas/identidad. El ciclo 1 ya está en `main` (mergeado), así que 2b se ramifica sobre `main` directamente; el bypass `DEV_USER_*` local sigue disponible para desarrollar sin tokens.
  - **Anticipar hueco (no implementar):** controles destino+formato del destilado (ciclo 4), costes visibles (ciclo 5), backoffice (ciclo 6). i18n dimensionado (strings externalizados, español por defecto).
  - **NO desplegar a prod nada a medias:** `main` está desplegable y desplegado; cualquier trabajo de 2b que rompa debe ir en rama hasta estar listo (patrón del ciclo 1).
- **Evidencia del estado (para reconciliar al arrancar):**
  - Programa: `docs/cambios/20260716_profesionalizacion-marketplace/DESIGN.md` + `BRIEF-marketplace-agustin.md` (+ `.docx`) + `INVESTIGACION-modelo-destino-prompt-guides.md`.
  - Ciclo 1: `ciclo-1-identidad-entra/{DESIGN,SPEC,RUNBOOK-entra-y-cutover}.md` (DESIGN con ADDENDUM de review). Código en `main`. **Sin rama pendiente** (`identidad-entra` borrada).
  - Ciclo 2: `ciclo-2-frontend-mobile-first/DESIGN.md`. **Cero código de ciclo 2** todavía.
  - `main` = `ea37a65` (o posterior si hubo redeploy de docs); árbol limpio.

## Qué se hizo esta sesión

1. **Brief de Azure Marketplace para Agustín** (niveles de oferta, requisitos, beneficios) — `BRIEF-marketplace-agustin.md` + `.docx`.
2. **DESIGN del programa** cerrado; **verificación S1** (Claude como destilador): **NEGATIVA** — CCU de Claude en Foundry es consumo Marketplace, no cubierto por suscripción sponsored → **GPT sigue de destilador**; Claude-destilador relegado.
3. **Ciclo 0 — investigación prompt-guides** completa y persistida (GPT+Claude cubiertos con fuentes oficiales; **Gemini y optimizadores NO cubiertos** → top-up pendiente en ciclo 4).
4. **Ciclo 1 `identidad-entra`** completo: análisis → spec → implementación → review adversarial independiente (bucle 3↔4) → **cutover a producción** → verificado e2e.
5. **Ciclo 2 `frontend-mobile-first`** — análisis cerrado con DESIGN.md (sub-división 2a/2b; stack ratificado).

## Qué se verificó CON EVIDENCIA REAL

- **Ciclo 1 — tests:** `npm test` **14/14** (allowlist + token-verify + regresión `db.test.js`).
- **Ciclo 1 — arranque local + Azure-simulado:** script 12/12 (públicos 200; protegidos sin token → 401 UNAUTHENTICATED; token basura → 401 TOKEN_INVALID; `/api/prompts` protegido tras el fix H1).
- **Ciclo 1 — review independiente:** veredicto **LIMPIO** tras corregir H1 (`/api/prompts` sin `identity`) + hardening H4 (`assertScope`) + enmiendas H2/H3/H5.
- **Ciclo 1 — cutover en PROD (verificado vía curl):** `/api/auth-config` y `/api/health/db` → **200**; `/api/config|sessions|prompts|diagnostics` sin token → **401 UNAUTHENTICATED** (JSON de la app); token basura → **401 TOKEN_INVALID** (jose+JWKS vivo).
- **Ciclo 1 — e2e de login REAL (usuario, en navegador):** login con `jesus.lopez@xenix.es` OK, **23 sesiones visibles** → identidad nueva + **backfill R3** confirmados; sin prompt de consentimiento (admin consent aplicado).
- Deploy: GitHub Actions run `29644754067` OK; merge `8f83f55`; cierre `ea37a65`.

## Configuración de producción tocada (para saber qué hay montado)

- **App Entra `speech-to-prompt`** (`f6e24391-f8eb-45a8-890d-70e618c5514e`): multi-tenant+MSA, tokens **v2**, SPA redirects (prod + localhost), scope `access_as_user`, **pre-autorización** de la SPA + **admin consent** Xenix.
- **App Service `speech-to-prompt-xenix`** (RG `rg-speech-to-prompt`): **Easy Auth DESACTIVADO** (`authsettingsV2 platform.enabled=false`). App Settings nuevas: `ENTRA_CLIENT_ID`, `ENTRA_API_AUDIENCE` (acepta `api://{id}` y `{id}`), `ENTRA_API_SCOPE`, `ENTRA_AUTHORITY` (`.../common`), `ALLOWED_EMAILS` (`jesus.lopez@xenix.es,agustin.hernandez@xenix.es`).
- **BD prod** `db-speech-to-prompt`: migración `006` aplicada; users id=1 backfilled a `3b1870f6-….f8cb98f1-…` + `tenant_id`. XENIX_TID = `3b1870f6-ff96-440e-9d46-a3db343eae1c`.
- **App vieja `speech-to-prompt-xenix`** (`e2ddebca-…`, single-tenant): era la de Easy Auth, ya no gatea.
- **Rollback ciclo 1** (si algún día hiciera falta): `authsettingsV2 platform.enabled=true` (backup `authV2-backup.json` — estaba en el scratchpad de la sesión, efímero; si no está, reconstruible: clientId `e2ddebca-…`, issuer `sts.windows.net/{XENIX_TID}/v2.0`, `RedirectToLoginPage`) + revertir merge (SHA previo `d3fffde`).

## Cómo retomar (próxima sesión)

1. **Reconciliar:** leer la línea "Fase actual" de CLAUDE.md y esta bitácora; confirmar que `main` sigue limpio/desplegado y que no hay ramas pendientes.
2. **Arrancar 2a (diseño):** decidir vía Claude Design (usuario) o artifacts (Claude Code). Empezar por **dirección visual + design system ligero** (tokens color/tipografía/espaciado + inventario de componentes), luego **maquetas de las pantallas clave** (login, captura, revisión, destilado —con hueco para los controles del ciclo 4—, resultado, historial, ajustes) y **decidir wizard-vs-pantalla-única**. Acotar qué pantallas/estados entran como "clave" (DESIGN §6 pregunta abierta 3).
3. Cuando el diseño esté elegido → **`/jcc-spec` del 2b**.

## Pendientes / notas para releer en frío

- **RUTA CRÍTICA (no depende de Claude): burocracia de Partner Center de Agustín** — verificación de cuenta + perfil fiscal + payout, obligatorios para publicar como transactable, con semanas de latencia. Recordárselo.
- **Refinamiento del troceo (observación de Jesús, sin formalizar):** la configuración de Marketplace/Partner Center (crear oferta, planes/precio, technical config, certificación) quizá merezca un **ciclo/track propio** explícito, distinto del ciclo 0 (fiscal/payout) y del ciclo 3 (código de integración). Decidir al llegar al ciclo 3.
- **Ciclo 4 (destilado-destino):** top-up de investigación pendiente para **Gemini** y **optimizadores/metaprompting oficiales** (no sobrevivieron a la verificación); + mini-eval de destino sobre prompts cortos.
- **Ciclo 3 (futuro), hueco H6 anotado:** `users.email` sigue `NOT NULL`; al **retirar la lista blanca** e introducir el gate de suscripción, un token sin claim de email haría fallar el INSERT. Abordar entonces.
- **Decisión "en caliente" a releer en frío:** el cutover del ciclo 1 lo ejecutó Claude directamente por CLI (el usuario lo delegó). Todo verificado, pero conviene que el usuario dé un repaso tranquilo a la config de Azure cuando pueda (App Settings, estado de Easy Auth, la app vieja `speech-to-prompt-xenix` que podría desactivarse/archivarse en el futuro para no dejar cabos).
- **Limpieza cosmética antigua pendiente** (de antes): UI de Ajustes muestra proveedores legacy; se resolverá con el rediseño del ciclo 2.

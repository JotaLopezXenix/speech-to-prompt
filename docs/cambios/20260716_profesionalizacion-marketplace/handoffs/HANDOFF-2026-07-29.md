# HANDOFF — Sesión 29-jul-2026 · Saneo documental del programa (enmiendas de la auditoría)

Bitácora de cierre. Foto fechada; la fuente de verdad del estado vivo es la línea "Fase actual" de `CLAUDE.md`. **Sesión sin código**: su producto son documentos. Va emparejada con [`HANDOFF-2026-07-29-recuperacion.md`](HANDOFF-2026-07-29-recuperacion.md), escrito en esta misma sesión para cubrir las tres sesiones que se habían quedado sin bitácora (23-jul tarde, 27-jul, 28-jul).

## Estado metodológico

- **Fase actual (al cerrar):** programa `profesionalizacion-marketplace`, **ciclo 3 `marketplace-transactable` ACTIVO**, **entre Fase 2 y Fase 3**. DESIGN cerrado (+ADDENDA). **SPEC-01 cerrado y en prod.** **SPEC-02 especificado con su provisión Azure ejecutada y verificada, pero sin implementar** (`src/services/fulfillment/` no existe — glob, 29-jul). Specs 03–06 sin escribir. **No hay trabajo JCC a medias**: esta sesión abrió y cerró un saneo documental completo.
- **Siguiente command:** **`/jcc-implement` de SPEC-02** (fulfillment secretless). Lo decide el usuario; SPEC-03 ya está desbloqueado (ver restricciones).
- **Restricciones activas (no saltar):**
  - **Al implementar SPEC-02, el §8.3 peldaño 2 (intercambio federado) es la prueba que da sentido al spec** y **solo se puede probar con código desplegado**: Kudu no recibe `IDENTITY_ENDPOINT`. No se puede declarar el secretless probado antes de eso.
  - **Las llamadas reales a las Fulfillment APIs devolverán 401/403 por diseño** hasta grabar tenant + app ID en la *Technical configuration* de la oferta (§6.6). No es un fallo del cliente.
  - ~~**Smoke de Blob del §8.4 PENDIENTE**~~ → **✅ ejecutado por el usuario el 30-jul y funciona**: reproducir un tramo en Revisión (el método anterior, Reprocesar sobre sesión nueva, era inalcanzable). **§8.4 cerrado**; de la verificación de SPEC-02 solo queda el §8.3 peldaño 2.
  - **[E]9 auto-activation OFF** (decidida hoy): SPEC-03 construye la landing **funcional** (token → login → resolve → match estricto → activate). El webhook `Subscribe` es *notify-only* y no crea accesos por sí solo.
  - **Antes del go-live:** quitar o gatear el parámetro `systemPrompt` de `distill` (ver Backlog). No es urgente pero no puede llegar al Marketplace.
  - **Preservar** (ciclos previos): `token-verify.js` (frontera de seguridad), aislamiento por `owner_id`, contrato `req.user`, salvaguardas de captura R1, invariante secretless.
- **Evidencia del estado (artefactos y su estado):**
  - **Nuevo:** `handoffs/HANDOFF-2026-07-29-recuperacion.md` y este handoff.
  - **Enmendados:** `CLAUDE.md` (saneo completo), `DESIGN.md` del programa (+ADDENDUM 29-jul), `ciclo-3/DESIGN.md` (+ADDENDUM 29-jul, [E]9, S1–S3 cerrados), `ciclo-3/SPEC-02` (+ADDENDUM 29-jul, §2, §4.3), `ciclo-3/RUNBOOK` (fila G, fila I, §3, §5, bitácora), `ciclo-3/BRIEF-…-23` (H-10, P7), `ciclo-3/BRIEF-…-29` (reunión cancelada), `ciclo-2/SPEC-04` (residuo), `README` del programa, `docs/cambios/README.md`.
  - **La `AUDITORIA-integridad-documental-2026-07-28.md` no se toca**: queda como registro histórico del contraste. Sus enmiendas están ejecutadas y así lo dice ahora el README.
  - **git:** `main == origin/main` en **`28fdea5`**. **NADA COMMITEADO en esta sesión**: 10 ficheros modificados + 1 nuevo en el árbol de trabajo. Esta foto es **pre-commit**.

## Qué se hizo

**Entrada:** el informe de auditoría del 28-jul (15 hallazgos, 9 enmiendas propuestas en su §6, ninguna ejecutada). **Salida:** las 9 ejecutadas, cada una citando su hallazgo.

1. **SPEC-02 (H-01/H-02/H-07/H-12/H-13)** — ADDENDUM 2026-07-29. Se **retira** la afirmación falsa «no existe reproductor de audio en la UI» y el método sustituto **Reprocesar**, inalcanzable; el smoke de Blob vuelve al método del §8.4 original (reproducir un tramo en Revisión). Fecha del ADDENDUM as-built corregida **27→28-jul**. Añadido `activate 400 = Suspended` a la tabla del §4.3. Precisada la versión de `@azure/identity`.
2. **DESIGN del programa (H-03)** — ADDENDUM 2026-07-29: el supuesto «los datos ya existen en `usage_events`» es **cierto para el LLM y falso para el STT** (~70 % del coste). El ciclo 5 debe **arreglar la medición antes** de construir la superficie, por la vía que recomienda el ANALISIS §2 (enviar la duración que ya mide el frontend, no instalar ffmpeg). Marcadores ⚠️ en la fila del ciclo 5 y en el §6.
3. **CLAUDE.md (H-04/H-05)** — saneo **completo**, más allá de lo listado: una sola verdad temporal (cutover hecho, `/` sirve `web/dist`, `public/` inexistente, sin editor de system prompt), «Fase actual» reducida a puntero corto, Backlog repoblado, desambiguados **los dos SPEC-02** (ciclo 2 `api-tipada` vs ciclo 3 `fulfillment-secretless`), corregido «on-disk audio» y documentados los **dos prefijos** `/api` y `/api/v1`.
4. **Handoff de recuperación (H-06)** — cubre 23-jul tarde (deploy de SPEC-01 con su evidencia de BD y su smoke real), 27-jul (SPEC-02 + ANALISIS + retirada de `ALLOWED_EMAILS` + volcado de la reunión) y 28-jul (provisión as-built + auditoría).
5. **README del programa (H-05)** y **índice global** — provisión ejecutada, enmiendas ejecutadas, handoffs registrados.
6. **Fechas (H-07)** — corregidas en SPEC-02 y en la fila I del RUNBOOK, con la nota de que es la 2ª reincidencia sobre una regla que ya existe.
7. **DESIGN ciclo 3 (H-08)** — ADDENDUM con el cierre de **auto-activation** contra fuente primaria y la **decisión [E]9: OFF**; añadida al §5. De paso, **S1/S2/S3 pasan de «a confirmar» a cerrados** con su matiz (S2 sigue pendiente de compra real en Preview). BRIEF del 29 reencabezado: reunión cancelada, qué preguntas ya responde la doc y cuáles siguen vivas.
8. **Investigación Marketplace no persistida (H-09)** — referencia sustituida por la auditoría §3 (con URLs) en el DESIGN del ciclo y en el RUNBOOK §3, que además gana dos hechos que no constaban citables: *metering exige flat rate* y *el pricing model es lo irreversible*.
9. **Higiene** — residuo de tool-call eliminado de `SPEC-04:366-367` (H-11); pitch del BRIEF-23 corregido con nota al pie (H-10: «ve el coste de cada operación» describía una capacidad que el producto no tiene).

## Qué se verificó CON EVIDENCIA REAL

**Re-verificado en esta sesión** (no heredado del informe — se contrastó antes de escribir cada enmienda):

- **H-01 refutado en su origen:** reproductor de audio en `web/src/routes/Review.tsx:46-91` (`Audio`, `togglePlay`, `getSegmentAudio` en :79) y chips `playable` en :142.
- **H-02 confirmado:** `canReprocess = item.has_audio && !item.has_transcription` en `web/src/routes/History.tsx:224`, render en :249.
- **H-04 confirmado:** `public/` **no existe** (`ls`); `server.js:91` sirve `web/dist` estático y `:79-84` redirige `/app`→`/` (302); `grep systemPrompt web/src` → solo `schema.d.ts`; el override sigue vivo en `src/routes/distill.js:55-57` y `/api/prompts` sigue montado (`server.js:50,69`) **sin consumidor**; el front envía solo `mode` (`Distill.tsx:44`) y `limpio` es el default (`:20`).
- **Cadena de auth del cliente tipado:** `setTokenProvider` instalado por `web/src/auth/AuthProvider.tsx:107` (MSAL) y `:121` (devBypass null).
- **H-11/H-12:** residuo verificado con `tail | cat -A` antes y después (diff = exactamente −2 líneas, CRLF intacto); `package.json:21` → `^4.5.0`.
- **El pipeline de despliegue NO aplica migraciones** (`azure-deploy.yml`: solo `npm ci` + build de Vite) → migrar/sembrar es secuencia manual. Esto es lo que permite acotar qué pudo ser el «fix del orden migrate+seed».
- **git:** `6691fd2` tocó **solo `CLAUDE.md`** (1 fichero, 1 línea) → el «fix» no fue de código. Fechas de todos los commits del hueco por `git log --date=iso-strict`. `ANALISIS` creado en `9d8ee4d` (28-jul 11:09) con encabezado «27-jul».
- **`npm test` → 16/16** tras todos los cambios (no había código que romper; se ejecuta como red de seguridad).

**NO verificado en esta sesión (heredado explícitamente de la auditoría del 28-jul, marcado como tal en los documentos):** las medidas contra **BD de producción** (migración 007 a las 16:40:35Z, 2 filas de `entitlements`, `audio_seconds` NULL 32/32, clave de precio desalineada), contra **Azure** (`az`: app de Entra, FIC, UAMI, App Settings, `createdDateTime`) y contra **producción** (curl, hash del bundle). No se re-midieron; el handoff de recuperación marca la procedencia dato a dato con `[aud]`.

**Hueco que se creía irreconstruible y se cerró con evidencia (lo más importante de la sesión):** el «**fix del orden migrate+seed**» del commit `6691fd2` era **una caída de producción de ~8 h el 23-jul**. Apareció en la memoria de proyecto y **no se dio por bueno sin corroborar**:

- `.github/workflows/azure-deploy.yml:9-13` → `on: push: branches: [main]`: **cualquier push a `main` despliega a producción** [cód]. El handoff del 23-jul declaraba «pusheado, **SIN desplegar**» — era falso, y esa creencia es la causa raíz.
- `gh run list`: el push de `eb21a2c..d20d304` desplegó el gate el **23-jul 08:31:27→08:33:19Z**, mientras la migración 007 no se aplicó hasta las **16:40:35Z** [aud] → ~8 h con `dbo.entitlements` inexistente → **500 `IDENTITY_FAILED` en toda petición autenticada** (no 403: el fallo era de esquema, no de autorización).
- Encaje fino que cierra el círculo: la concesión pre-login de Agustín se creó a las **17:10Z** [aud] y el commit `6691fd2` es de las **17:14:38Z** [git] — 4 minutos después.

**Backport hecho, no solo narrado (era el punto 3 del cierre):** el `SPEC-01` recibió un **ADDENDUM 2026-07-29 de despliegue as-built** que documenta el incidente y corrige **dos defectos del §6** que seguían vigentes y que iban a heredarse al desplegar SPEC-02: (1) el paso «desplegar el código» no existe como acción separada — el orden correcto es **migrar antes de PUSHEAR**; (2) el §6 predecía un 403 degradado donde en realidad hay un 500 general. Además quedan escritos ahí dos gotchas operativos reales: migrar contra Azure **no es `npm run migrate`** (cargaría el `.env` local) y el seed de cutover **no cubre a quien nunca se ha logueado** (`FROM dbo.users`).

## Cómo retomar (próxima sesión)

1. **Reconciliar:** "Fase actual" de `CLAUDE.md` + este handoff + el de recuperación + `RUNBOOK` + índice global. Confirmar si los cambios de esta sesión se commitearon (al cerrar **no lo estaban**).
2. **`/jcc-implement` de SPEC-02**: leer el spec **incluido su ADDENDUM 2026-07-29** (el bullet de Blob del ADDENDUM as-built está retirado: no seguirlo). Al desplegar, ejecutar por fin el §8.3 peldaño 2.
3. **Aprovechar la ventana**: el smoke de Blob (reproducir un tramo en Revisión) se puede hacer en cualquier momento con sesión logueada; cierra la regresión de la provisión.
4. **Papeleo, sin esperar a Microsoft**: el tax del Seller (W-8BEN-E) va por asesor fiscal; la reunión se retomó a ~1 mes.

## Addendum 30-jul-2026 — dos cierres posteriores a esta bitácora

Trabajo de ops del día siguiente, anotado aquí para no abrir una bitácora nueva por dos acciones de minutos (la fecha se marca explícitamente para no arrastrarla):

1. **Smoke de Blob ejecutado por el usuario → §8.4 de SPEC-02 CERRADO.** La reproducción de un tramo en Revisión funciona en producción. La regresión completa de la provisión queda verde; de la verificación de SPEC-02 solo sobrevive el §8.3 peldaño 2.
2. **Secreto residual de Easy Auth eliminado** (pendiente que esta sesión había puesto en el Backlog, y que aquí se **poda**). Comprobaciones **previas** al borrado: `az webapp auth show` → **`enabled: false`** (Easy Auth desactivado a nivel de plataforma) y **cero referencias en código** a los dos settings. Borrados `MICROSOFT_PROVIDER_AUTHENTICATION_SECRET` y `WEBSITE_AUTH_AAD_ALLOWED_TENANTS`. Regresión post-reinicio: `/api/v1/health/db` → **200 `{"ok":true}`** (0,24 s), `/` → **200**, `/api/v1/sessions` sin token → **401**. **Efecto de fondo:** el App Service ya no tiene **ningún** App Setting con un secreto (los 19 restantes son endpoints, IDs públicos, nombres de recurso y flags) → el invariante *secretless* pasa a ser literal.
   *De paso, una corrección: `ALLOWED_EMAILS` — que es lo que se creía pendiente — ya no existía; se retiró el 27-jul (`a6983bd`). Lo que quedaba era el secreto de Easy Auth, que es otra cosa.*

## Decisiones tomadas "en caliente" (releer en frío)

- **[E]9 auto-activation OFF.** Se eligió por control de facturación y por preservar el match estricto de [E]1, no por facilidad: con ON habría menos código. Si al re-agendar la reunión Microsoft recomienda ON para self-service, la decisión se revisa — es un toggle por plan, pero **[E]1 tendría que rediseñarse como reconciliación post-facto** (mecanismo que ya existe: el binding pre-login por email de SPEC-01).
- **Override de system prompt: retirado, no aplazado.** Se cerró la puerta a "recuperarlo en el ciclo 4" como capacidad de producto; lo que el ciclo 4 diseñará es destino × formato, que es otra cosa. Si en frío se echa de menos el bucle rápido de afinado sobre un dictado real, la alternativa vigente es git + `seed-prompts`.
- **La auditoría no se reescribe.** Se decidió dejarla intacta como foto del contraste y marcar en el README que sus enmiendas están ejecutadas, en lugar de convertirla en un documento vivo con checkboxes.
- **Se corrigieron dos cosas que la auditoría no listaba** (los dos prefijos `/api`/`/api/v1`, y el «on-disk audio» del workaround de Groq) porque el mandato de esta sesión era *una sola verdad temporal* en CLAUDE.md, no solo los hallazgos.

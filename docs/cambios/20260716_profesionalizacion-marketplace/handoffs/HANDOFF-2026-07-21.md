# HANDOFF — Sesión 21-jul-2026 · Programa `profesionalizacion-marketplace`

Bitácora de cierre. Foto fechada con evidencia; la fuente de verdad del estado vivo es la línea "Fase actual" de `CLAUDE.md`.

> Nota de fecha: los artefactos de SPEC-02/03 quedaron internamente fechados "20-jul" (arrastre del contexto de la sesión previa); la sesión se cerró el 21-jul. Drift cosmético, sin efecto.

## Estado metodológico

- **Fase actual:** ciclo 2 `frontend-mobile-first`, sub-ciclo **2b (construcción)**, en **implementación**. **3 de 6 SPEC CERRADOS** (01 cimiento · 02 API tipada · 03 auth/login), los tres desplegados y verificados en Azure. **SPEC-04 aún sin DESIGN/SPEC.**
- **Siguiente command que toca:** **`/jcc-spec` del SPEC-04** (captura + salvaguardas, **R1 — el hueso del ciclo**). Los SPEC 04–06 se escriben just-in-time.
- **Restricciones activas (no saltar):**
  - **Decisiones estructurales de 2b (cerradas):** app en **`web/`** (package.json propio; backend intacto en la raíz); **entrega incremental a `main` + cutover final** (prod sirve el `public/` viejo en `/`; el nuevo en **`/app`** temporal); troceo en 6 SPEC.
  - **Stack (ADDENDUM SPEC-01):** React 19 + Vite 8 + TS 6 + **Tailwind v4 CSS-first** (tokens en `web/src/index.css`, **sin** `tailwind.config.ts`) + shadcn + react-router 7 + oxlint.
  - **API tipada (SPEC-02):** contrato spec-first en `openapi/speech-to-prompt.yaml` (fuente de verdad); tipos generados a mano con `npm run gen:api` (**`openapi-typescript` vía `npx`, NO devDep** — peer TS5 vs proyecto TS6; nunca en build/CI); cliente `web/src/api/client.ts` (`openapi-fetch`, `baseUrl:/api/v1`).
  - **Auth (SPEC-03):** `@azure/msal-react`+`sessionStorage`+redirect; `useAuth()` unifica devBypass|MSAL; costuras `setTokenProvider`/`setUnauthorizedHandler` del cliente tipado ya cableadas.
  - **Qué se PRESERVA:** frontend viejo en `/` (incl. `public/js/auth.js`) intacto; backend `/api/*` + alias `/api/v1` + `identity` intactos; contrato de sesión y flujo de 4 fases; **salvaguardas de captura** (no perder audio, banner Reintentar, warm-up, sin falsos positivos — **objeto de SPEC-04, R1**); Ajustes/Historial; identidad bearer del ciclo 1; **devBypass local**.
  - **No desplegar a medias:** `main` desplegable; lo que rompa va en rama.
- **Evidencia del estado (para reconciliar al arrancar):**
  - `main` = `a307aeb`; árbol limpio; **sin ramas pendientes** (`2b-01/02/03-*` borradas, mergeadas).
  - Diseño 2a: `ciclo-2-frontend-mobile-first/{DESIGN,DESIGN-2a,DESIGN-SYSTEM-2a,BRIEF-claude-design-2a}.md` + `diseno-claude-design/`.
  - SPEC escritos: `SPEC-01_cimiento.md`, `SPEC-02_api-tipada.md`, `SPEC-03_auth-login.md`. **SPEC-04…06 NO escritos.**
  - Código en `main`: `web/` (cimiento + `src/api/` + `src/auth/`), `openapi/speech-to-prompt.yaml`, bloque `/api/v1` en `server.js`.
  - Azure (prod): `/` = frontend viejo; `/app/` = SPA nuevo con login MSAL real; `/api/*` y `/api/v1/*` vivos.

## Qué se hizo esta sesión

Dos SPEC completos de punta a punta (especificar → implementar → revisar → desplegar → verificar):

1. **SPEC-02 · API tipada — CERRADO.** OpenAPI 3.1 spec-first a mano (`openapi/speech-to-prompt.yaml`, 16 ops) + alias `/api/v1` aditivo en `server.js` (mismos routers, mismo orden de `identity`) + cliente tipado en `web/src/api/` (`openapi-fetch` dep + `openapi-typescript` vía npx; `schema.d.ts` versionado; fachada `api` + costura de token). `Capture.tsx` migrado a `api.healthDb()`. Review independiente **LIMPIA**. Merge ff (`eb86d58`+`dd03792`), deploy run `29766600118` OK, verificado en Azure.
2. **SPEC-03 · auth/login MSAL — CERRADO.** `web/src/auth/` (`authContext`/`config`/`msal`/`AuthProvider`/`RequireAuth`) con `@azure/msal-react`; gating de rutas; avatar+logout reales; cableado token/401 al cliente tipado. Review independiente **LIMPIA** (0 ALTA/MEDIA; 4 BAJA sin acción). Ops Entra (redirect URI `…/app/` añadido vía `az rest`, aditivo). Merge ff (`1d927c6`+`847ab17`), deploy run `29770125032` OK. **Login real e2e verificado por el usuario en prod.**

## Qué se verificó CON EVIDENCIA REAL

**SPEC-02:**
- Local: `gen:api` exit 0 e idempotente (`schema.d.ts` commiteado == regenerado); `web` build (tsc+vite)+lint verdes; `npm ci` reproducible (tras regenerar el lock); `npm test` raíz **14/14**; smoke del alias sin BD (`/api/v1/*`≡`/api/*`).
- Azure (prod, curl, tras absorber ~15s de lag de restart): `/api/v1/health/db`→`{ok:true}` 200 (BD real), `/api/v1/auth-config`→config MSAL, `/api/v1/sessions` sin token→401 `UNAUTHENTICATED`, token basura→401 `TOKEN_INVALID`; `/`→viejo, `/app/`→nuevo.

**SPEC-03:**
- Local: `web` build+lint verdes; `npm test` raíz **14/14**; `npm ci` reproducible (lock regenerado); **render e2e de la rama devBypass en navegador** (backend local sin BD): sin pantalla de login → `/app/capture`, avatar **"DEV"**, logout oculto, cliente tipado llama `/api/v1`, **0 errores de consola**. (Ojo: el 1er load sirvió el build cacheado del SW de la PWA → "JL" viejo; tras limpiar SW+caches, "DEV" correcto.)
- Review independiente ejecutó build/lint/test + render e2e devBypass → **LIMPIO**.
- Azure: deploy OK; `/app/` sirve el build nuevo (`index-Crjz0441.js`, MSAL empaquetado); `/api/v1/auth-config` = MSAL (no devBypass); **el usuario hizo login real e2e OK** (ve la pantalla de Login y entra autenticado).

## Cómo retomar (próxima sesión)

1. **Reconciliar:** leer "Fase actual" de `CLAUDE.md` + esta bitácora; confirmar `main` limpio/desplegado y sin ramas pendientes.
2. **Arrancar SPEC-04** con `/jcc-spec` (el más delicado, R1): portar `public/js/{audio-recorder,audio-guards,diagnostics}.js` (lógica de navegador) y **re-expresar la orquestación de salvaguardas** de `public/js/phase1-capture.js` a React **preservando comportamiento** (no perder audio ante corte externo, banner Reintentar/Descartar, warm-up de BD, telemetría `diagnostic_events`, sin falsos positivos en parada intencional). **Cablear al cliente tipado** `addSegment` (multipart) y `getSegmentAudio` (stream) — **quedaron diferidos de SPEC-02**: el contrato ya está en `openapi/` y en `schema.d.ts`, pero **sin fachada en `client.ts`**; se añaden aquí. Verificación = repro equivalente de la parada externa (vía Playwright, como en su ciclo original) + no-pérdida de audio.
3. Seguir el troceo: 05 resto del flujo → 06 marco (Historial/Ajustes reales) → **cutover final** (`vite base` y `basename` de `/app`→`/`, retirar el bloque `/app` de `server.js` y `public/`; el redirectUri MSAL pasa a `origin+/`, ya registrado el origen raíz en Entra).

## Pendientes / notas para releer en frío

- **Diferido de SPEC-02 → hacer en SPEC-04:** fachada tipada de `addSegment` (multipart, `bodySerializer` para FormData) y `getSegmentAudio` (respuesta binaria `audio/webm`) en `web/src/api/client.ts`.
- **Gotcha recurrente (lock de `web/`):** `npm install` incremental desincroniza `web/package-lock.json` (faltan `@emnapi/*`) y rompe `npm ci` en CI. Fix: `cd web && rm -rf node_modules package-lock.json && npm install`, validar con `npm ci`. Pasó en SPEC-02 y SPEC-03.
- **PWA service worker:** tras un deploy, `/app` puede servir el build **cacheado** (viejo). Al verificar en navegador: incógnito o hard-reload / limpiar SW+caches. No es bug.
- **Lag de restart de Azure App Service** (~15s tras deploy "success"): re-sondear un endpoint/asset que solo exista en el commit nuevo antes de concluir. Memoria: `project_azure_deploy_restart_lag`.
- **Entra app `f6e24391…`:** SPA redirect URIs ahora = `[http://localhost:3000, https://…azurewebsites.net, https://…azurewebsites.net/app/]`. Al cutover, `/app→/` usará el origen raíz (ya registrado). Si se desarrolla MSAL en local, falta registrar `http://localhost:5173/app/`.
- **4 BAJA de la review de SPEC-03 (sin acción, releer en frío por si molestan):** `handling401` no se resetea en `client.ts` (autocura en cualquier navegación); doble-bootstrap del `AuthProvider` en StrictMode **dev** (prod no re-invoca efectos); bundle 574 kB por MSAL sin code-splitting; nombres de claves i18n divergen del texto del SPEC (autoconsistentes).
- **Node 20 deprecado** en las actions (`checkout@v4`/`setup-node@v4`, forzadas a Node 24) → bump menor cuando toque.
- **Sin commit/push pendiente:** todo en `main` y en `origin/main` (`a307aeb`). El último push desencadenó un redeploy docs-only (inofensivo).

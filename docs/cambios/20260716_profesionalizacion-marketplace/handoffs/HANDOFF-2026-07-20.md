# HANDOFF — Sesión 20-jul-2026 · Programa `profesionalizacion-marketplace`

Bitácora de cierre. Foto fechada con evidencia; la fuente de verdad del estado vivo es la línea "Fase actual" de `CLAUDE.md`.

## Estado metodológico

- **Fase actual:** ciclo 2 `frontend-mobile-first`, sub-ciclo **2b (construcción)**. **2a (diseño) COMPLETO.** **SPEC-01 `cimiento` CERRADO** (implementado → revisado limpio → desplegado → R5 verificado en Azure). Ciclo 2b abierto: **1 de 6 SPEC hecho**.
- **Siguiente command que toca:** **`/jcc-spec` del SPEC-02** (API tipada: OpenAPI de `/api/*` + alias aditivo `/api/v1` + cliente tipado en `web/`). Los SPEC 02–06 se escriben **just-in-time**.
- **Restricciones activas (no saltar):**
  - **Decisiones estructurales de 2b (cerradas, no reabrir):** app en **`web/`** (subcarpeta con package.json propio; backend intacto en la raíz); **entrega incremental a `main` + cutover final** (prod sirve el `public/` viejo en `/` hasta el cutover; el frontend nuevo vive en **`/app`** temporal); troceo en 6 SPEC (01 cimiento · 02 API tipada · 03 auth/login MSAL · 04 captura+salvaguardas · 05 resto flujo · 06 marco).
  - **Stack REAL (ADDENDUM en SPEC-01, ratificado en mesa común):** React 19 + Vite 8 + TS 6 + **Tailwind v4 (CSS-first)** + shadcn/ui + react-router 7 + oxlint. Tailwind v4 = tokens en `web/src/index.css` (`@import "tailwindcss"` + `@theme` + `@custom-variant dark`), **sin** `tailwind.config.ts` ni `postcss.config.js`.
  - **Design system "B · Papel":** los componentes leen **tokens** (semánticos shadcn mapeados), nunca hex. Choque de nombres resuelto: pino→`primary`, pino-soft→`accent`. Valores en `DESIGN-SYSTEM-2a.md`.
  - **Qué se PRESERVA** (ciclo + SPEC-01): frontend viejo en `/`; backend `/api/*` + gating `identity`; flujo de 4 fases y contrato de sesión; **salvaguardas de captura** (no perder audio, banner Reintentar, warm-up, sin falsos positivos — se re-expresan en SPEC-04, R1, mayor riesgo); Ajustes/Historial; identidad bearer del ciclo 1.
  - **No desplegar a medias:** `main` desplegable; el trabajo que rompa va en rama hasta estar listo.
- **Evidencia del estado (para reconciliar al arrancar):**
  - Diseño 2a: `ciclo-2-frontend-mobile-first/{DESIGN-2a,BRIEF-claude-design-2a,DESIGN-SYSTEM-2a}.md` + `diseno-claude-design/` (9 snapshots verbatim de Claude Design + `support.js`).
  - SPEC-01: `ciclo-2-frontend-mobile-first/SPEC-01_cimiento.md` (con ADDENDUM del stack). Código en `web/` (en `main`).
  - Ciclo (encuadre): `ciclo-2-frontend-mobile-first/DESIGN.md`. Programa: `../DESIGN.md`.
  - `main` = `622e48c` (o posterior si hubo redeploy de docs); árbol limpio; **sin ramas pendientes** (`2b-01-cimiento` borrada, mergeada).
  - **SPEC-02…06: NO escritos aún** (just-in-time).

## Qué se hizo esta sesión

1. **2a diseño — COMPLETO.** Se sembraron 3 direcciones visuales (artifact) y se eligió **"B · Papel"**. El usuario produjo el diseño completo (7 pantallas + Design System) en **Claude Design** (división de trabajo: Claude Design = visual, Claude Code = desarrollo). **Handoff vía DesignSync**: el proyecto es de **tipo normal** (`projectId 76b4739c-0c3f-444a-ad54-94d74e90a572`) → **NO sale en `list_projects`**, se accede por `projectId`. 9 ficheros bajados a `diseno-claude-design/` + destilado legible en `DESIGN-SYSTEM-2a.md`. Fuentes decididas: **Source Serif 4** (display) + **system-ui** (UI) + **IBM Plex Mono** (datos).
2. **2b análisis/spec.** Descompuesto en 6 SPEC; decisiones estructurales cerradas; **SPEC-01 escrito**.
3. **SPEC-01 implementado** en `web/`: design system CSS-first, PWA, i18n (es), shell + stepper + navegación guiada libre, 7 pantallas placeholder, delta backend (`server.js` sirve `web/dist` en `/app` con guarda `existsSync`) + CI (`azure-deploy.yml` build de `web/`) + `.gitignore`/`package.json`. Stack moderno subido a mesa común (ADDENDUM).
4. **Review adversarial INDEPENDIENTE** (subagente que no escribió el código): veredicto **LIMPIO**; 1 hallazgo BAJA (`--card`→`surface-elevated`) corregido; nota del icono PWA anotada.
5. **Desplegado + R5 verificado en Azure.** Merge ff a `main`; el 1er deploy falló por `web/package-lock.json` desincronizado (`npm ci` estricto) → corregido; 2º deploy verde.

## Qué se verificó CON EVIDENCIA REAL

- **Build/lint/tests (local):** `web` `npm run build` verde (tsc + vite; PWA `manifest.webmanifest`+`sw.js`; fuentes woff2 **self-hosted**); `oxlint` 0 errores (3 warnings benignos en primitivos shadcn); `npm test` raíz **14/14**.
- **Servido local (Express + curl):** `/app/`→SPA nuevo (`/app/assets/*.js`+manifest), `/app/capture`→SPA (deep-link), `/`→**frontend viejo** (`/js/app.js`+`app-header`). Regresión OK.
- **Render e2e (navegador in-app):** shell + stepper + Captura montan; i18n en español; **"● Backend conectado"** (`/api/health/db` 200); **0 errores de consola**.
- **Review independiente:** subagente ejecutó build+lint+test+servido; veredicto LIMPIO.
- **CI/deploy (Azure):** run `29761469430` **OK (2m4s)**, paso "Build web (Vite)" verde. (Antes: run `29761074026` FALLÓ por `npm ci`/lock → corregido en `dc3cf90`.)
- **R5 en Azure (prod, curl)** — host `speech-to-prompt-xenix-hnc4ccfbfkdcdjem.westeurope-01.azurewebsites.net`: `/app/`=**200** (SPA nuevo), `/`=**200** (viejo), `/app/settings`=**200** (deep-link), `/app/icon.svg`=**200**, `/api/health/db`=**200** (backend vivo; Oryx no se rompió con `web/` presente).

## Cómo retomar (próxima sesión)

1. **Reconciliar:** leer "Fase actual" de `CLAUDE.md` + esta bitácora; confirmar `main` limpio/desplegado y sin ramas pendientes.
2. **Arrancar SPEC-02** con `/jcc-spec`: OpenAPI del contrato `/api/*` existente + **alias aditivo `/api/v1`** (backend, NO reescritura) + **cliente tipado** en `web/` (candidato: `openapi-typescript` + `openapi-fetch`). Ramificar sobre `main`. El placeholder de Captura ya consume `/api/health/db` como patrón de llamada.
3. Seguir el troceo: 03 auth/login MSAL → 04 captura+**salvaguardas (R1)** → 05 resto flujo → 06 marco → **cutover final** (`/` pasa a servir `web/dist`, `vite base`+`basename`→`/`, se retira el bloque `/app` y `public/`).

## Pendientes / notas para releer en frío

- **Decisión "en caliente":** el stack se movió a moderno (React 19 / Vite 8 / **Tailwind v4 CSS-first**) durante la implementación porque el scaffolder los instaló; ratificado y en ADDENDUM. Releer en frío que **no hay `tailwind.config.ts`**: los tokens viven en `web/src/index.css`.
- **BD local sin migración `006`:** `GET /api/sessions` da 500 `tenant_id` en local; correr `npm run migrate` en local si se desarrolla contra la BD. **Prod OK.**
- **Icono PWA:** se sirve un único `icon.svg` (instalable), no PNG 192/512 → pulido menor (anotado en ADDENDUM de SPEC-01).
- **Workflow deploy:** `checkout@v4`/`setup-node@v4` usan Node 20 deprecado (forzado a 24) → bump menor cuando toque.
- **Lección de proceso:** instalar deps de forma incremental dejó el `package-lock.json` desincronizado y rompió `npm ci` en CI; regenerar el lock (`rm package-lock.json && npm install`) y validar con `npm ci` en local antes de pushear.
- **RUTA CRÍTICA independiente (recordar a Agustín):** burocracia de Partner Center (verificación de cuenta + fiscal + payout) — semanas de latencia, obligatoria para publicar como transactable.
- **Ciclo 4 (futuro):** top-up de investigación Gemini/optimizadores; el hueco destino+formato ya está representado en el diseño ("Próximamente" en Destilado), y coste en Resultado (ciclo 5).

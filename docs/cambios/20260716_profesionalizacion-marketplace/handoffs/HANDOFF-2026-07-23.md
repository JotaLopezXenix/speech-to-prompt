# HANDOFF — Sesión 23-jul-2026 · Ciclo 3: DESIGN + SPEC-01 (spec→implement→review, veredicto SÍ)

Bitácora de cierre. Foto fechada con evidencia; la fuente de verdad del estado vivo es la línea "Fase actual" de `CLAUDE.md`. Continúa a `HANDOFF-2026-07-22-ciclo3-arranque.md` (arranque del ciclo 3).

> **Nota de fecha (reconciliación):** los encabezados de `DESIGN.md`/`SPEC-01`/`REVIEW-SPEC-01` y el handoff de arranque dicen "22-jul", pero el trabajo se **commiteó el 23-jul** (arrastre de fecha). Cosmético; esta bitácora va con la fecha real (23-jul). Implicación: **la reunión Microsoft ISV Success (23-jul) es HOY.**

## Estado metodológico

- **Fase actual (al cerrar):** Programa `profesionalizacion-marketplace`, **ciclo 3 `marketplace-transactable` ACTIVO**. **DESIGN del ciclo cerrado.** **SPEC-01 (modelo de acceso + gate) completó el ciclo spec→implement→review con veredicto SÍ**, commiteado y **pusheado** (`d20d304`), **SIN desplegar**. No hay trabajo JCC a medias.
- **Siguiente command / paso:** **desplegar SPEC-01** (paso de ops, no command JCC) y/o **`/jcc-spec` de SPEC-02** (fulfillment secretless). **02 y 06 mejor tras la reunión de MS (hoy)**; 03/04/05 en cualquier momento. Lo decide el usuario.
- **Restricciones activas (no saltar):**
  - **Deploy de SPEC-01 — ORDEN CRÍTICO:** `npm run migrate` + ejecutar `scripts/sql/seed-entitlements-cutover.sql` **ANTES de desplegar** el código nuevo del gate; **retirar el App Setting `ALLOWED_EMAILS`** después; smoke logueado. Si se despliega antes del seed → Jesús/Agustín reciben **403 `NO_ACCESS`** hasta sembrar.
  - **Decisiones estructurales del ciclo** en `DESIGN.md §5` (identidad-comprador "misma cuenta" · entitlement unificado marketplace+manual · fulfillment secretless por credencial federada · retención v1-A). **F-1 aceptado** (ADDENDUM en el DESIGN): la mitigación de la *provisión de no-suscriptores en `users`* se decide al abrir la oferta pública.
  - **Preservar** (ciclos previos): `token-verify.js` (frontera de seguridad), aislamiento por `owner_id`, contrato `req.user`, salvaguardas de captura R1, red privada secretless. El gate por suscripción ya **retiró** `ALLOWED_EMAILS` (en código; en Azure, quitar el App Setting tras desplegar).
  - **Confirmaciones pendientes para la reunión de MS (hoy):** viabilidad de la **credencial federada** (fulfillment secretless), **superficie de cancelación** del cliente, **auto-activation** vs resolve/activate.
- **Evidencia del estado (artefactos y su estado):**
  - En `ciclo-3-marketplace-transactable/`: `DESIGN.md` (+ ADDENDUM F-1), `SPEC-01_modelo-acceso-gate.md`, `REVIEW-SPEC-01.md` (veredicto SÍ + cierre de bucle 3↔4), `RUNBOOK-partner-center.md`, `BRIEF-reunion-isv-success-2026-07-23.md` (+ `.docx`), `ARQUITECTURA.md`. **SPEC-02…06 no existen aún.**
  - Código SPEC-01 (commiteado + pusheado): `migrations/007_entitlements.sql`, `src/services/entitlement-store.js`, `src/middleware/identity.js` (gate), `scripts/sql/seed-entitlements-cutover.sql`, `test/entitlement.test.js`; **eliminados** `src/utils/allowlist.js` + `test/allowlist.test.js`. `CLAUDE.md` actualizado (esquema `entitlements` + gate; corregida mención stale a Easy Auth). OpenAPI + `schema.d.ts` → `NO_ACCESS`.
  - **git:** `main == origin/main` en **`d20d304`**, árbol limpio. Todo pusheado. **Nada desplegado a Azure** esta sesión.

## Qué se hizo esta sesión

1. **DESIGN del ciclo 3** (entrevista socrática 2ª tanda) → `DESIGN.md`: decisiones estructurales [E] + flujos + superficie de regresión + riesgos + troceo en 6 specs.
2. **SPEC-01** (modelo de acceso + gate) escrito.
3. **Implementado SPEC-01:** migración 007 (`entitlements` + índices + fix H6), `entitlement-store.js`, gate en `identity.js` (retira allowlist), seed de cutover, tests. Commit `3e45802`.
4. **Review adversarial independiente** (subagente, contexto fresco): **veredicto SÍ**. F-2 (drift OpenAPI `NOT_ALLOWLISTED`→`NO_ACCESS`) **corregido** + regenerado `schema.d.ts`; F-1 (provisión de no-suscriptores) **aceptado y anotado** como ADDENDUM; F-3/4/5 aceptadas. Commit `d20d304`.
5. **Push** de los 4 commits (`eb21a2c..d20d304`).

## Qué se verificó CON EVIDENCIA REAL

- `npm test` → **16/16** (en implementación y **re-verificado** tras el fix F-2). `test/allowlist.test.js` retirado; `test/entitlement.test.js` (6 casos) añadido.
- `npm run migrate` aplicó la **007**; re-ejecutar → *Sin migraciones pendientes* (idempotente).
- **Sanity del store contra SQL real:** `bind` vincula ✔ · activa→true ✔ · caducada→false ✔ · sin concesión→false ✔ (script temporal, ya borrado).
- **Review independiente** (subagente): `npm test` 16/16 reproducido; regresión verde (token-verify/`req.user`/aislamiento/bypass-dev/rutas intactos); drift F-2 verificado por grep.
- F-2 corregido: `web/src/api/schema.d.ts` regenerado, **sin `NOT_ALLOWLISTED`** (grep).
- **NO verificado (es deploy-time):** 403 `NO_ACCESS` logueado en Azure, smoke logueado, ejecución del seed en prod. Pendiente del despliegue.

## Pendientes

- **Deploy de SPEC-01** (orden crítico migrate+seed **antes**; retirar `ALLOWED_EMAILS`; smoke logueado).
- **Specs 02–06** (02 fulfillment secretless + 06 Preview **tras la reunión**; 03/04/05 cuando sea).
- **Reunión Microsoft ISV Success (HOY 23-jul):** brief + docx + arquitectura listos. Volcar respuestas en `BRIEF-…§6` y en el runbook; confirmar credencial federada / superficie de cancelación / auto-activation.
- **Burocracia (runbook):** tax del **Seller 87879330** "Action required" + asignación de payout por defecto.
- **Cross-cutting heredados:** prueba §8.3 coldstart-sql; bump Node 20→24; limpieza `/app→/` + alias `/api/*`; IP de Agustín en firewall SQL/Storage; smoke `mejorar-destilado-limpio`.
- **Doc-debt menor:** encabezados "22-jul" en DESIGN/SPEC-01/REVIEW vs. commit real 23-jul (cosmético; ofrecí corregir).

## Cómo retomar (próxima sesión)

1. **Reconciliar:** "Fase actual" de `CLAUDE.md` + este handoff + `RUNBOOK-partner-center.md` + índice global. Confirmar `main == origin` en `d20d304`.
2. **Tras la reunión de MS:** volcar respuestas; luego `/jcc-spec` de **SPEC-02** (fulfillment secretless) con la credencial federada confirmada, y/o **desplegar SPEC-01**.
3. **Deploy de SPEC-01** respetando el orden crítico (migrate + seed **antes** de desplegar).

## Decisiones tomadas "en caliente" (releer en frío)

- **SPEC-01 cierra limpio** (review SÍ); **listo para desplegar pero NO desplegado**.
- **F-1 aceptado para v1** (mitigación de la provisión de no-suscriptores → al abrir la oferta pública).
- **Fecha real 23-jul** (docs de la sesión etiquetados 22-jul por arrastre; **la reunión es HOY**).
- Fulfillment **nativo** + **secretless por credencial federada** (cert plan B) reafirmados.
